<section class="state-unavailable" role="status" aria-labelledby="state-unavailable-heading">
  <h2 id="state-unavailable-heading">Reading state unavailable</h2>
  <p>No paper is inferred to be unread or unstarred while reading state is unavailable.</p>
</section>

<style>
  .state-unavailable {
    padding: clamp(1.25rem, 5vw, 2rem);
    border: 1px solid var(--border);
    border-radius: 0.4rem;
    background: var(--surface);
  }

  .state-unavailable h2 {
    margin-bottom: 0.75rem;
  }

  .state-unavailable p {
    max-width: 38rem;
    margin-bottom: 0;
    color: var(--muted);
  }
</style>
